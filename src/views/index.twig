{% extends 'layout.twig' %}


{% block body %}
    <div class="container align-content-center" >
        <h1>{{ title }}</h1>
        <p>Welcome to {{ title }}</p>
        {% include 'caleandar.twig' %}
        {% include 'weather.twig' %}

    </div>

{% endblock %}